# sporty — critique log

> [CRITIQUE-RUBRIC.md](../../CRITIQUE-RUBRIC.md) の記録フォーマットで各版を記録。

## v1 採点 2026-06-10

**対象**: STRIDE ランニングクラブ LP（Hero + 統計3カード + 写真3カード）/ Figma frame `138:100` / figma-bridge 構築
**比較参照**: refs 2枚（TENISTA / NIKE広州）＋ FAMBOX軸2 視覚語彙

| 観点 | 点 | コメント |
|---|---|---|
| 1 色ゾーニング | 2/2 | bg白 / surface薄グレー / accent #FF4D1F は帯と92%の2点のみ — 1ビビッド規律通り |
| 2 タイポ階層 | 2/2 | Display 88 uppercase × 統計56 × ラベル11 — ジャンプ率6.3xが出ている |
| 3 グリッド | 2/2 | フルブリードヒーロー＋本文マージン80＋3等分24ガター。ズレなし |
| 4 コンポーネント | 2/2 | 統計カード3枚同型（HUG化で corporate v1 の余白ムラ問題を回避）、写真カード同型、ピルCTA |
| 5 余白 | 2/2 | S16/M32/L64 等比リズム。カード HUG 化により均一 |
| 6 らしさ | 1/2 | **減点**: 色帯×文字の深度（裾が帯に潜る）は実現したが、**写真不在のため「運動量の説得」が未達**。sporty は corporate より人間3割（写真差込）への依存が構造的に大きい |

**合計: 11/12 ／ 構造系0点: 無 ／ 判定: 昇格条件達成（宮川さん最終確認待ち — 写真差込後の見え方確認を推奨）**

**人間仕上げメモ（採点対象外の3割）:**
- **写真差込が決定打**: ヒーローのダーク面 → モーションブラーのランナー写真。Display の文字を被写体の奥に潜らせるマスク処理（TENISTA 型）
- 写真3カードに実画像＋ **±3〜6° 回転**（bridge に rotate ツールなし）
- デュオトーン処理（NIKE 型）／ letter-spacing .1em ／ コンデンス書体化

**v1 で確認できた重要知見:**
- **z-order の罠が深度演出に使える**: `move_node` で文字を帯の位置に重ねると文字が帯の**奥**に潜った（兄弟ノードの描画順は作成順で、後から動かしても変わらない）。意図せず NIKE 型「帯が前を横切る」構造になった → 前後関係を作りたい順に**作成順を設計**するのが正攻法
- corporate との構造差: sporty の70%は「骨格＋色帯＋統計」まで。**「らしさ」の最後の1点は写真が握る** → LOOP の Generate 完了条件は「構造7割」のままで正しいが、sporty 系パターンは昇格判定を写真差込後に行う運用が良い（CRITIQUE-RUBRIC 改訂候補）

**純構築時間: 5分59秒**（11:54:46→12:00:45 / レシピ作成除く・自己修正2回込み）
→ corporate 4分15秒 比 +1分44秒（絶対配置ヒーローの調整コスト）。

---

## v1-html チャネル実験 2026-06-10（宮川さん提案: HTML ベースで起こす）

**対象**: [prototypes/stride-v1.html](prototypes/stride-v1.html)（同一 SYSTEM.md・同一題材 STRIDE を HTML/CSS で構築）

| 観点 | Figma (v1) | HTML (v1) | 差の理由 |
|---|---|---|---|
| 1 色ゾーニング | 2 | 2 | 同等（CSS variables = SYSTEM.md トークン直結） |
| 2 タイポ階層 | 2 | 2 | HTML は Oswald（コンデンス）+ letter-spacing 指定可 → bridge の Inter 固定制約が消える |
| 3 グリッド | 2 | 2 | 同等。HTML は clamp() でレスポンシブまで含む |
| 4 コンポーネント | 2 | 2 | 同等＋hover 状態（scale/translateY）まで実装済み |
| 5 余白 | 2 | 2 | 同等 |
| 6 らしさ | **1** | **2** | **HTML はランナーSVGシルエット（デュオトーン+残像ブラー）・文字の前後深度・±回転・マーキー帯・設計図グリッド下地まで AI 側で実装できた** |

**合計: Figma 11/12 → HTML 12/12**

**チャネル実験の結論:**
- sporty のような**写真・深度・動勢依存のパターンは HTML チャネルの方が AI 7割の到達点が高い**。
  figma-bridge で「人間3割」に残した項目（フォント選択 / letter-spacing / 回転 / デュオトーン / 深度）が
  ほぼすべて AI 側に移った。残る人間3割は「実写真の差込」と「シルエット→実画像の置換」のみ。
- Figma チャネルの強みは「デザイナーが後から編集できる」こと。HTML チャネルの強みは「表現再現度と速度」。
- **運用提案**: パターンごとに推奨チャネルを SYSTEM.md に明記する（corporate=どちらでも / sporty=HTML 優位）。
  Step 2 以降の非デザイナー運用では HTML 出力の方が「そのまま見せられる」点でも有利。
- 検証環境: launch.json `design-wheel-prototypes`（ただし既存 wf-preview サーバー再利用のため
  `docs/okr/wf_simple_diagnosis_20260513/stride-v1.html` に symlink で配信。恒久化するなら専用 server 設定を整理）。
